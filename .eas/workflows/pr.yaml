name: PR Checks

on:
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: Lint & Type Check
    type: managed
    steps:
      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npx expo lint

      - name: Type Check
        run: npx tsc --noEmit
